_format_version: "3.0"
_transform: true

services:
- name: my-service
  url: https://example.com
  plugins:
  - name: kong-rs-log
    config:
      my_field: "abcd"
  routes:
  - name: my-route
    paths:
    - /
